# nixrc playground (MVP)

Static playground shell with:

- Editor + output split pane
- Console panel
- Example scripts dropdown
- Run button + auto-run debounce
- Share via URL hash (`#code=...`)

Open `playground/index.html` in a browser from a local static server.
